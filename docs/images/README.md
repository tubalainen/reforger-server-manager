# README screenshots

These images are referenced by the top-level [`README.md`](../../README.md).
Filenames are stable — to refresh a screenshot, replace the file in place with
the same name and the README updates automatically.

| File | Shows |
|------|-------|
| `01-server-templates.png` | Server Templates list page |
| `02-template-scenario.png` | Edit template → Scenario step, with the live `config.json` preview |
| `03-template-mods.png` | Edit template → Mods step (the mod manager) |
| `04-template-settings.png` | Edit template → Settings step |
| `05-instance-detail.png` | A running server instance: status bar, scheduled restarts, live log |

Keep them reasonably sized (PNG, roughly 1200–1400 px wide is plenty).
